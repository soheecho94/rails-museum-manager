<div class="add">
  <% if user_signed_in? %>
    <%= link_to "Remove", category_path(@category), method: "delete" %>
  <% end %>
</div>

<div class="show">
  <h2><%= @category.name %></h2>
</div>

<div id="<%= @category.id %>" class="artwork_list">
  <ul>
    <!-- <% @category.artworks.each do |artwork| %>
      <li>
        <%= link_to "#{artwork.title} |  #{artwork.artist} | #{artwork.year}", museum_artwork_path(artwork.museum, artwork) %>
      </li>
    <% end %> -->
  </ul>
</div>
